# Deprecated web version

This directory is an archived snapshot of the original local browser application.

It receives no new features, design updates, or routine maintenance. All future development belongs in `desktop/`. The files remain available only for reference and compatibility with existing local use.
